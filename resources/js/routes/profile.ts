export const edit = () => '/settings/profile';
export const update = () => '/settings/profile';
export const destroy = () => '/settings/profile';

export const profile = {
    edit,
    update,
    destroy,
};

export default profile;
